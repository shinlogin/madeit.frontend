<%= form_for(@store_register) do |f| %>
  <% if @store_register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_register.errors.count, "error") %> prohibited this store_register from being saved:</h2>

      <ul>
      <% @store_register.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
