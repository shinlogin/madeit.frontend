<%= form_for(@store_policies_ad) do |f| %>
  <% if @store_policies_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_policies_ad.errors.count, "error") %> prohibited this store_policies_ad from being saved:</h2>

      <ul>
      <% @store_policies_ad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
