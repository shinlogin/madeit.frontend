<%= form_for(@store_policy) do |f| %>
  <% if @store_policy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_policy.errors.count, "error") %> prohibited this store_policy from being saved:</h2>

      <ul>
      <% @store_policy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
