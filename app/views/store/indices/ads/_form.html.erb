<%= form_for(@store_indices_ad) do |f| %>
  <% if @store_indices_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_indices_ad.errors.count, "error") %> prohibited this store_indices_ad from being saved:</h2>

      <ul>
      <% @store_indices_ad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
