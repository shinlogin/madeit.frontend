<h1>Listing store_indices</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @store_indices.each do |store_index| %>
  <tr>
    <td><%= link_to 'Show', store_index %></td>
    <td><%= link_to 'Edit', edit_store_index_path(store_index) %></td>
    <td><%= link_to 'Destroy', store_index, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Index', new_store_index_path %>
