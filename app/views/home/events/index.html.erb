<h1>Listing home_events</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_events.each do |home_event| %>
  <tr>
    <td><%= link_to 'Show', home_event %></td>
    <td><%= link_to 'Edit', edit_home_event_path(home_event) %></td>
    <td><%= link_to 'Destroy', home_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_home_event_path %>
