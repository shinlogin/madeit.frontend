<%= form_for(@home_store) do |f| %>
  <% if @home_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_store.errors.count, "error") %> prohibited this home_store from being saved:</h2>

      <ul>
      <% @home_store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
