<h1>Listing home_stores</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_stores.each do |home_store| %>
  <tr>
    <td><%= link_to 'Show', home_store %></td>
    <td><%= link_to 'Edit', edit_home_store_path(home_store) %></td>
    <td><%= link_to 'Destroy', home_store, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Store', new_home_store_path %>
