<h1>Listing home_cart_indices</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_cart_indices.each do |home_cart_index| %>
  <tr>
    <td><%= link_to 'Show', home_cart_index %></td>
    <td><%= link_to 'Edit', edit_home_cart_index_path(home_cart_index) %></td>
    <td><%= link_to 'Destroy', home_cart_index, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Index', new_home_cart_index_path %>
