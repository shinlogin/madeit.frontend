<%= form_for(@home_cart_index) do |f| %>
  <% if @home_cart_index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_cart_index.errors.count, "error") %> prohibited this home_cart_index from being saved:</h2>

      <ul>
      <% @home_cart_index.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
