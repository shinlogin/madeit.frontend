<%= form_for(@home_indices_product) do |f| %>
  <% if @home_indices_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_indices_product.errors.count, "error") %> prohibited this home_indices_product from being saved:</h2>

      <ul>
      <% @home_indices_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
