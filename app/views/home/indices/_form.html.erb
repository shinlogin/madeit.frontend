<%= form_for(@home_index) do |f| %>
  <% if @home_index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_index.errors.count, "error") %> prohibited this home_index from being saved:</h2>

      <ul>
      <% @home_index.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
