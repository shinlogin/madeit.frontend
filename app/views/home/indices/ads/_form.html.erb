<%= form_for(@home_indices_ad) do |f| %>
  <% if @home_indices_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_indices_ad.errors.count, "error") %> prohibited this home_indices_ad from being saved:</h2>

      <ul>
      <% @home_indices_ad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
