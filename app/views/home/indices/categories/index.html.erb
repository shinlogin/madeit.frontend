<h1>Listing home_indices_categories</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_indices_categories.each do |home_indices_category| %>
  <tr>
    <td><%= link_to 'Show', home_indices_category %></td>
    <td><%= link_to 'Edit', edit_home_indices_category_path(home_indices_category) %></td>
    <td><%= link_to 'Destroy', home_indices_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_home_indices_category_path %>
