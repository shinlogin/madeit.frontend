<h1>Listing home_searches_ads</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_searches_ads.each do |home_searches_ad| %>
  <tr>
    <td><%= link_to 'Show', home_searches_ad %></td>
    <td><%= link_to 'Edit', edit_home_searches_ad_path(home_searches_ad) %></td>
    <td><%= link_to 'Destroy', home_searches_ad, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ad', new_home_searches_ad_path %>
