<%= form_for(@home_searches_message) do |f| %>
  <% if @home_searches_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_searches_message.errors.count, "error") %> prohibited this home_searches_message from being saved:</h2>

      <ul>
      <% @home_searches_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
