<%= form_for(@home_search) do |f| %>
  <% if @home_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_search.errors.count, "error") %> prohibited this home_search from being saved:</h2>

      <ul>
      <% @home_search.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
