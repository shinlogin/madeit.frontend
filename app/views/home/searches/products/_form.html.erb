<%= form_for(@home_searches_product) do |f| %>
  <% if @home_searches_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_searches_product.errors.count, "error") %> prohibited this home_searches_product from being saved:</h2>

      <ul>
      <% @home_searches_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
