<h1>Listing home_categories_categories</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_categories_categories.each do |home_categories_category| %>
  <tr>
    <td><%= link_to 'Show', home_categories_category %></td>
    <td><%= link_to 'Edit', edit_home_categories_category_path(home_categories_category) %></td>
    <td><%= link_to 'Destroy', home_categories_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_home_categories_category_path %>
