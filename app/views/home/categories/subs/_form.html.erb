<%= form_for(@home_categories_sub) do |f| %>
  <% if @home_categories_sub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_categories_sub.errors.count, "error") %> prohibited this home_categories_sub from being saved:</h2>

      <ul>
      <% @home_categories_sub.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
