<h1>Listing home_categories_subs</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_categories_subs.each do |home_categories_sub| %>
  <tr>
    <td><%= link_to 'Show', home_categories_sub %></td>
    <td><%= link_to 'Edit', edit_home_categories_sub_path(home_categories_sub) %></td>
    <td><%= link_to 'Destroy', home_categories_sub, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sub', new_home_categories_sub_path %>
