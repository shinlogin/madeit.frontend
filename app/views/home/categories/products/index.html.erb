<h1>Listing home_categories_products</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_categories_products.each do |home_categories_product| %>
  <tr>
    <td><%= link_to 'Show', home_categories_product %></td>
    <td><%= link_to 'Edit', edit_home_categories_product_path(home_categories_product) %></td>
    <td><%= link_to 'Destroy', home_categories_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product', new_home_categories_product_path %>
