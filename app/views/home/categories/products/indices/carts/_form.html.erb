<%= form_for(@home_categories_products_indices_cart) do |f| %>
  <% if @home_categories_products_indices_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_categories_products_indices_cart.errors.count, "error") %> prohibited this home_categories_products_indices_cart from being saved:</h2>

      <ul>
      <% @home_categories_products_indices_cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
