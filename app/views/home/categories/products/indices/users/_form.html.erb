<%= form_for(@home_categories_products_indices_user) do |f| %>
  <% if @home_categories_products_indices_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_categories_products_indices_user.errors.count, "error") %> prohibited this home_categories_products_indices_user from being saved:</h2>

      <ul>
      <% @home_categories_products_indices_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
