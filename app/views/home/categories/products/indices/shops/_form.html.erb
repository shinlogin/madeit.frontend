<%= form_for(@home_categories_products_indices_shop) do |f| %>
  <% if @home_categories_products_indices_shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_categories_products_indices_shop.errors.count, "error") %> prohibited this home_categories_products_indices_shop from being saved:</h2>

      <ul>
      <% @home_categories_products_indices_shop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
